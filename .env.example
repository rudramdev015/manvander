# URL of the Node/Express API (see server/README.md for how to deploy it).
# Leave empty during local dev before the backend exists yet - the site
# falls back to its built-in static content automatically.
VITE_API_URL=https://your-api.onrender.com
